# Figure provision after the empirical relocation

9 September 2026. The current article has six figures containing eleven trees and five tables. Both supplements retain their generated numerical tables. This menu assesses that existing provision before proposing additions. No images were inspected or new figures built in this stage.

| Candidate | Kind and claim | Form and source | Priority / disposition |
|---|---|---|---|
| 1. Same some across dependent/independent uses | Conceptual: distinguishes lexical category, phrase type and function | Existing four-panel Forest Figure 1 | Must; retain. |
| 2. A genitive NP in Det function | Conceptual: shows nested nominal determination and the outer lexical head | Existing Forest Figure 2, Kim's book | Must; retain. |
| 3. Modifier selection within the proposed Noun | Conceptual: almost modifies every while experienced modifies teacher | Existing Forest Figure 3 | Must; retain. |
| 4. The partitive head and domain | Conceptual: wine is inside the PP; some heads the outer projection | Existing Forest Figure 4 | Must; retain. |
| 5. Modified few under both Head analyses | Conceptual: contrasts ordinary Head with genuine Mod–Head fusion | Existing paired Forest Figure 5 | Must; retain. |
| 6. Compound modifier domains | Conceptual: compares one Nom domain with the rival's DP–Nom distinction | Existing paired Forest Figure 6 | Must; retain. |
| 7. Taxonomy-by-headedness grid | Conceptual: makes the two independent choices spatially explicit | Possible two-by-two replacement for Table 5; existing rules and labels only | Nice if fresh readers confuse the dimensions. The current three-column table already states them. Do not add a duplicate figure now. |
| 8. Two NPs, two permission checks | Conceptual: distinguishes article Det permission from apple argument permission | Possible annotated the apple tree beside §7.2; existing fragment | Nice if fresh readers lose the two heads. The current worked paragraph explains this and Figure 2 supplies the nesting pattern. |
| 9. Coordinate versus nested Noun | Conceptual: shows the optional intermediate pronoun grouping | Possible paired taxonomy trees replacing selected Table 1 rows | Stretch; the table already compares six arrangements without implying that extra hierarchy is intrinsically costly. |
| 10. Matrix sensitivity | Data: compares label agreement across starts and feature sets | `analysis/results/kgroups-single-starts.csv` and `kgroups-summary.csv`; distribution plot with separately marked best-objective fit | Stretch, supplement only. The present four-row table answers the audit's numerical question. A plot must distinguish optimum criterion from label agreement and claim no population interval. |
| 11. Corpus function counts | Data: displays the pinned annotation inventory | `analysis/results/corpus-manifest.json`; simple count bars | Stretch, supplement only. The existing count table is clearer for lookup; bars would not estimate productivity or validate a taxonomy. |

Retain the six existing figures. Consider candidates 7–9 only in response to a specific fresh-reader failure. No new data collection or decorative causal/cluster diagram is justified by the current reading. Any conceptual replacement should use the existing Forest/category-function notation; any numerical plot must use the cited outputs and house plotting style.

This is the planning stage only. There is no dependent build waiting for a selection.
