Follow task.md and task.json. Assign a required evidence_type to each of the 110 supplied participation claims, and return assignments.json matching schema.json. This is source extraction from the two full documents embedded in task.json; no Lean formalization is requested. Each assignment needs an exact source quotation in basis_quote. Where the sources do not determine the basis, return not_determinable rather than guessing.
