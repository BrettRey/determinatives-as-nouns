# Summary of changes for run e1eab78d-045b-4ed8-827c-80365e51aa67
I read both complete source documents embedded in `task.json` — the manuscript *Determinatives as nouns in English* (source M) and the supplement *Quantificational controls: attestations and provenance* (source Q) — and assigned exactly one required `evidence_type` to each of the 110 supplied participation claims. The result is `assignments.json` at the project root; it validates against `schema.json` (110 items, no additional properties), and every `basis_quote` was programmatically verified to be an exact substring of the named source's full text, LaTeX markup included. No Lean work was produced, as requested, so there are no build targets to report.

How the assignments were derived. For the 54 supplement cells I worked from Table `tab:quant-controls` and its caption legend: G cells → `cgel_described`, R cells (bare argument `certain`, `various`) → `cgel_restricted`, *I* cells → `constructed_illustration`, numbered-reference cells → `retained_attestation` quoting the numbered example together with its footnoted provenance, and the four S cells (argument `certain` with a relative, bare existential `certain`, existential `various` with a relative and without a dependent) → `searched_not_found`, quoting the closing paragraph that names them. I did not treat the supplement's blanket NOW/COCA footnote as attestation backing for any cell; only cells with a specific retained example are `retained_attestation`, and searched-but-unattested cells are recorded as searched, not as exclusions. For the 56 manuscript claims I distinguished: patterns the manuscript attributes to *CGEL* (`cgel_described`); *CGEL* restrictions rather than bans, such as `plenty` resisting modification, `lot` permitting only limited modification, and predicative `Their enemies were many` being uncommon and formal (`cgel_restricted`); constructed positive examples with no corpus or *CGEL* attribution (`constructed_illustration`); examples marked with `\ungram{}` (`constructed_ungrammatical`); the three retained attestations with provenance — the Uber Drivers Forum and AULRO informal attributive examples and the English Web Treebank sentence for independent `both` (`retained_attestation`); and points the manuscript explicitly marks as its own analytical commitments, chiefly the determinative modifier attachments that Table `tab:existing`'s caption flags as such (`authors_analysis`).

Final distribution: 37 `cgel_described`, 25 `retained_attestation`, 23 `constructed_illustration`, 10 `authors_analysis`, 6 `constructed_ungrammatical`, 5 `cgel_restricted`, 4 `searched_not_found`, 0 `not_determinable`. Each assignment carries a `note` identifying the table column or passage relied on and, where relevant, flagging that a layered analytical commitment (for example an internal-versus-peripheral attachment) sits on top of a *CGEL*-sourced permission. I judged every claim's basis to be determinable from the two documents, so `not_determinable` was not used; the borderline cases — where a use is *CGEL*-attributed but the manuscript's attachment analysis is its own — are recorded in the notes so the choice can be checked. `responsibility_notice` is the empty string: I identified no epistemic, authorization, coordination, or human-oversight risk in this bounded, source-only field assignment, and the sources were not edited.