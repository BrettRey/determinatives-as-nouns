# Summary of changes for run 278f33b9-3642-4af7-98a1-f52c4fba3e49
I produced the requested exhaustive participation-claim census and wrote it to `census.json` in the project root, matching `schema.json`.

**What is in it.** 658 participation claims recorded across all 28 sections listed in `task.json`, worked through in the given order. Each claim records the expression, the construction or syntactic function, a status (`licensed`, `excluded`, `conditional`, `not_stated`), an `evidence_type` from the required enumeration, and a `quote` that is an exact substring of that section's text, LaTeX markup included; optional `condition` and `note` fields carry stated restrictions and clarifications. Per-section counts in `section_counts` are computed from the recorded claims, so they match exactly: e.g. `sec:independent` 78, `sec:fragment` 64, `sec:det-uniform` 55, `sec:adjectival-profile` 55, `sec:membership` 36, `sec:payne` 35, `sec:modification` 33, `sec:article-membership` 33, `sec:enough` 33. Two sections genuinely contain no participation claim — `sec:proposal` and `sec:economy`, both short framing passages — and are recorded as 0 with a note explaining why.

**Coverage beyond running prose.** Worked examples and formal displays were treated as claim sources: the bracketed subject/object/complement-of-preposition example set, the syntactic trees (including the fusion diagrams and the genitive, partitive, `almost every` and compound figures), the profile-comparison table, the matched-heads table, the fragment's permission table (every form by function cell), the four-implementation comparison table, the rival-accounts table, and the formal Det-category, category-condition and Det–Head/Mod–Head relation displays. Footnote claims (predeterminer and peripheral modifiers, completeness modifiers, coordination markers, informal attributive attestations, derivational and numeral remarks) are included. `evidence_type` classifies the basis the manuscript itself gives — CGEL citations as `cgel_described`/`cgel_restricted`, the manuscript's own proposals as `authors_analysis`, constructed and starred examples, preserved attestations, one reported search with no qualifying result, and `not_determinable` where the grounds are a third-party account.

**Verification.** A reproducible builder in `census_build/` assembles the census and refuses to emit it unless every quote is an exact substring of the corresponding section text, every status and evidence type is in the schema's enumerations, ids are unique, and the per-section counts equal the claims recorded. The output also validates against `schema.json` under JSON Schema 2020-12. The `responsibility_notice` states how the census was produced, that it reports rather than endorses the manuscript's claims, and that the source text was not edited. `census_build/README.md` documents the method. No Lean work was produced, as requested, so no Lake targets are claimed.