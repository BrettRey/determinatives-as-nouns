# Exhaustive participation-claim census

Follow task.json. The manuscript is supplied split into its sections. Record every participation claim in every section and return `census.json` matching schema.json, including a per-section count that matches the claims you recorded. There is no target number of claims. Do not replace the requested JSON with Lean work.
