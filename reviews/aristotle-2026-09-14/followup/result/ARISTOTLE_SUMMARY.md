# Summary of changes for run 059202f5-d1e1-4232-af68-993fdc517e5e
I re-did the audit from `fragment.tex`, producing a single standalone Lean 4 file `RequestProject/Fragment.lean` (no imports — core only; no `sorry`, no new axioms; every result depends on `propext` alone) and a 483-word `REPORT.md`.

Before proving anything, the file's header separates explicit source constraints (the permission table, form selection within the `no/none`, `my/mine`, `she/her` paradigms, the "intermediate Head relations require no further argument permission" clause, target restrictions, the one-Det NP core, and the two primary-category conditions) from what the tree representation already supplies (acyclicity and unique parentage are given by the datatype and are not audited as omissions) and from the additional modelling assumptions. Constituents are encoded structurally — NP cores with a Nom Head and at most one Det, which is itself an NP — with no stand-in flags. Modifier geometry is excluded, and nothing is claimed about peripheral-modifier geometry. Lexeme identity is kept separate from surface form, and the count/number of a determining expression is kept distinct from what it selects in its target; `some`'s phrase-level value is left unspecified rather than assigned to manufacture a witness.

Answer to the question: within the encoded scope the fragment does keep the two sets of permissions apart, in both directions. Every NP headed by `the` is proved inadmissible as subject and as object under both accounts, so admitting `the apple` as an argument confers no independent argument use on the embedded article phrase; and a mother's argument admissibility is proved to depend only on its own head, form, well-formedness and determination requirement. This is a consequence already explicit in the compositional definitions, and the report says so plainly. The source-grounded cases all come out as stated: `the apple` admitted, `Some left` admitted, `*Every arrived` rejected by use permission, `*Book arrived` rejected by the determination requirement only (its subject permission is proved to hold), `Books arrived` admitted.

The search did turn up something, but elsewhere. An exhaustive enumeration of all depth-2 trees yields 44 admitted subjects, including the two-word `*few few`, `*the she`, `*my mine`, `*no none`, `*the some`. For `*few few` every condition is displayed and every feature value is one the source supplies. The gap is that a Det's target is constrained by count/number only, with no condition on the target head's category. I formalised the condition the source's own prose supplies (pronoun-headed nominals take no Det; among determinative-headed targets only `few`, and only under definite determination) and proved it removes exactly those admissions while retaining `the few`, `the apple`, `every apple`, `some apples`, `my books`.

As a secondary check, recategorization equivalence holds: the category condition is satisfied by every form under both accounts, so admissibility is account-invariant, and `my/mine` and `she/her` are proved pronouns and primary Noun in both accounts (with executable checks), only determinatives changing primary category. Lean certifies these facts about the encoding, not that the encoding faithfully represents the grammar; faithfulness rests on the constraint audit in the file header.